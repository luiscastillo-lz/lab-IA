# ============================================================================
# CONFIGURACIÓN DE ENTORNO - LAB-IA
# Asistente Virtual para Laboratorio de Control de Calidad
# ============================================================================

# --- Google Gemini API ---
GOOGLE_API_KEY=tu_api_key_aqui
# Obtén tu API key en: https://aistudio.google.com/app/apikey

# --- PostgreSQL Database ---
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_DB=labia_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=admin171419860

# --- LLM Configuration (Google Gemini) ---
LLM_MODEL=gemini-2.5-flash
LLM_TEMPERATURE=0.8
# Temperatura: 0 (determinista) a 1 (creativo)
# Configurado: 0.8 para respuestas naturales

LLM_MAX_TOKENS=4096
# Máximo de tokens en la respuesta
# Configurado: 4096 para respuestas completas

EMBEDDING_MODEL=models/embedding-001
# Modelo de embeddings de Google

# --- Retrieval Configuration ---
RETRIEVAL_K=5
# Número de documentos a recuperar en similarity search
# Configurado: 5 documentos

# --- Chunking Configuration (para ingest.py) ---
CHUNK_SIZE=1024
# Tamaño de chunks en tokens

CHUNK_OVERLAP=150
# Overlap entre chunks en tokens

# --- Flask Configuration ---
PORT=8010
DEBUG=False
# Para desarrollo: DEBUG=True

# --- Vector Store ---
COLLECTION_NAME=labia_embeddings

# --- PDF Processing ---
PDF_INPUT_PATH=./raw

# ============================================================================
# INSTRUCCIONES DE USO:
# ============================================================================
# 1. Copia este archivo como .env en el directorio raíz del proyecto:
#    cp .env.example .env
# 2. Edita .env y reemplaza "tu_api_key_aqui" con tu Google API Key
# 3. Cambia POSTGRES_PASSWORD por una contraseña segura (opcional)
#
# 2. Edita .env y completa los valores reales:
#    - GOOGLE_API_KEY: Tu API key de Google AI Studio
#    - POSTGRES_PASSWORD: Contraseña segura para PostgreSQL
#
# 3. NUNCA subas el archivo .env a control de versiones (agregarlo a .gitignore)
#
# 4. Para validar la configuración:
#    python database.py  (inicializa DB)
#    python ingest.py --test --files "LLCCI02*.pdf"  (prueba ingesta)
#    python app.py  (inicia servidor)
# ============================================================================

# --- OPCIONAL: Configuración Avanzada ---

# Logging
LOG_LEVEL=INFO
# Niveles: DEBUG, INFO, WARNING, ERROR, CRITICAL

# OCR (si usas pytesseract)
# TESSERACT_CMD=C:\Program Files\Tesseract-OCR\tesseract.exe
# Solo necesario si Tesseract no está en PATH

# Java (si usas tabula-py)
# JAVA_HOME=C:\Program Files\Java\jdk-11
# Solo necesario si Java no está en PATH
